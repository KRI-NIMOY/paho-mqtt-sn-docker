--- MQTTSNGateway/src/linux/xbee/SensorNetwork.cpp.orig	2026-03-19 20:51:34.841883704 -0700
+++ MQTTSNGateway/src/linux/xbee/SensorNetwork.cpp	2026-03-19 20:52:47.245652163 -0700
@@ -214,11 +214,11 @@
 int XBee::broadcast(const uint8_t* payload, uint16_t payloadLen){
 	SensorNetAddress addr;
 	addr.setBroadcastAddress();
-	return send(payload, (uint8_t) payloadLen, &addr);
+	return send(payload, payloadLen, &addr);
 }
 
 int XBee:: unicast(const uint8_t* payload, uint16_t payloadLen, SensorNetAddress* addr){
-	return send(payload, (uint8_t) payloadLen, addr);
+	return send(payload, payloadLen, addr);
 }
 
 int XBee::recv(uint8_t* buf, uint16_t bufLen, SensorNetAddress* clientAddr)
@@ -327,14 +327,15 @@
 	return -1;
 }
 
-int XBee::send(const uint8_t* payload, uint8_t pLen, SensorNetAddress* addr){
+int XBee::send(const uint8_t* payload, uint16_t pLen, SensorNetAddress* addr){
 	D_NWSTACK("\r\n===> Send:    ");
     uint8_t checksum = 0;
     _respCd = -1;
 
     _serialPort->send(START_BYTE);
-    send(0x00);              // Message Length
-    send(14 + pLen);         // Message Length
+    uint16_t frameDataLen = 14 + pLen;
+    send((uint8_t)(frameDataLen >> 8));    // Message Length MSB
+    send((uint8_t)(frameDataLen & 0xFF));  // Message Length LSB
 
     _serialPort->send(API_XMITREQUEST); // Transmit Request API
     checksum += API_XMITREQUEST;
--- MQTTSNGateway/src/linux/xbee/SensorNetwork.h.orig	2026-03-19 20:51:34.841883704 -0700
+++ MQTTSNGateway/src/linux/xbee/SensorNetwork.h	2026-03-19 20:52:22.477032212 -0700
@@ -94,7 +94,7 @@
 private:
 	int readApiFrame(uint8_t* recvData);
 	int recv(uint8_t* buf);
-	int send(const uint8_t* payload, uint8_t pLen, SensorNetAddress* addr);
+	int send(const uint8_t* payload, uint16_t pLen, SensorNetAddress* addr);
 	void send(uint8_t b);
 
 	Semaphore _sem;
